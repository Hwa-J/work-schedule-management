import styled from 'styled-components';
import { StyledButton } from '../AddEventNomalModal/style';

export const ModalBody = styled.div.attrs({
  className: 'modal-body',
})`
  text-align: center;
  padding: 30px 0;
`;

export const Button = styled(StyledButton)``;
